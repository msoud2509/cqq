#include "circuit_generator.h"

namespace cqq::utils {

void generate_random_qasm_circuit(
    const std::string& filename, unsigned num_qubits, unsigned num_cregs, unsigned num_gates) {
    std::ofstream file(filename);

    }

} // namespace cqq::utils