# CQQ

A C++(20) project for compiling and simulating quantum circuits. 

## Usage
An example run is provided in `main.cc`. To compile and run the project, use the following commands:
```bash
make
make run
```
